sqlite3
.mode tabs
-- .import 'PATH/EBIRD-FILE.TXT' TABLE-NAME
.import '/Volumes/GoogleDrive/My Drive/Manacus/Manacus-eBird/ebd_CO-CAU_prv_relAug-2022/ebd_CO-CAU_prv_relAug-2022_provisional.txt' ebird
.tables --to see the tables
.mode column
.schema ebird

ALTER TABLE valle ADD COLUMN lat numeric;
UPDATE valle SET lat = CAST("LATITUDE" as numeric);

ALTER TABLE valle ADD COLUMN lon numeric;
UPDATE valle SET lon = CAST("LONGITUDE" as numeric);

ALTER TABLE valle ADD COLUMN dates real;
UPDATE valle SET dates = CAST("OBSERVATION DATE" as numeric);

.schema valle

create table tabla_nueva as select "TAXONOMIC ORDER" as "tax_order", "CATEGORY" as "category", "SCIENTIFIC NAME" as "sci_name", 
"COMMON NAME" as  "comm_name", "OBSERVATION COUNT" as "count", "COUNTRY" as "country", "STATE" as "state", "LATITUDE" as "lat", 
"LONGITUDE" as "lon", "OBSERVATION DATE" as "dates", "OBSERVER ID" as "observer", "TRIP COMMENTS" as comments from valle 
where ("OBSERVER ID" == "obsr436508") limit 10;